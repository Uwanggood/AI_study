# 상황: 배치 2개, RGB(3), 32x32 이미지
import torch

x = torch.randn(2, 3, 32, 32)

# 목표: 이미지를 4x4 크기의 패치로 쪼개세요.
# 32x32 이미지 -> 4x4 패치가 총 64개(8x8) 나옵니다.
# 최종 모양은 [2, 64, 48]이 되어야 합니다.
# (48 = 4*4*3, 패치 하나당 픽셀정보)
x = x.view(2, 64, 48)
print(x)